# === Independent AI (independentai.space) ===

# --- Database (Neon free tier — postgres, ZORUNLU) ---
DATABASE_URL=postgresql://user:pass@host.neon.tech/db?sslmode=require

# --- Auth (ZORUNLU, 32+ karakter random) ---
JWT_SECRET=change-me-openssl-rand-hex-32

# --- Vercel Cron koruması (ZORUNLU production'da) ---
CRON_SECRET=change-me-vercel-cron-only-knows-this

# --- AI provider API keys (opsiyonel — yoksa mock mode) ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# --- Sosyal giriş / OAuth (opsiyonel — ikisi de boşsa butonlar gizlenir) ---
# Google Cloud Console → OAuth client (Web). Yetkili yönlendirme URI'leri:
#   http://localhost:3200/api/auth/oauth/google/callback
#   https://independentai.space/api/auth/oauth/google/callback
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
# LinkedIn Developers → "Sign In with LinkedIn using OpenID Connect" ürünü.
# Yetkili yönlendirme URL'leri:
#   http://localhost:3200/api/auth/oauth/linkedin/callback
#   https://independentai.space/api/auth/oauth/linkedin/callback
LINKEDIN_OAUTH_CLIENT_ID=
LINKEDIN_OAUTH_CLIENT_SECRET=

# --- Public ---
NEXT_PUBLIC_SITE_URL=https://independentai.space
