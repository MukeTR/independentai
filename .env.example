# === Independent AI (independentai.space) ===

# --- Database (Neon free tier — postgres, ZORUNLU) ---
DATABASE_URL=postgresql://user:pass@host.neon.tech/db?sslmode=require

# --- Auth (ZORUNLU, 32+ karakter random) ---
JWT_SECRET=change-me-openssl-rand-hex-32

# --- Vercel Cron koruması (ZORUNLU production'da) ---
CRON_SECRET=change-me-vercel-cron-only-knows-this

# --- AI provider API keys (opsiyonel — yoksa mock mode) ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# --- Public ---
NEXT_PUBLIC_SITE_URL=https://independentai.space
